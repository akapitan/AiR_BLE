<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.seierfriendapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="18"
        android:targetSdkVersion="18" />
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyTheme"
        android:name="com.example.core.BaseApplication">

        <meta-data android:name="AA_DB_NAME" android:value="SeierFriendApp.db" />
        <meta-data android:name="AA_DB_VERSION" android:value="1" />

        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
                  android:icon="@android:color/transparent">
        </activity>
        <!--Register service -->
        <service android:name="com.example.backgroundScanning.ScaningService" android:enabled="true"/>
        <!--Register broadcast event -->
        <receiver android:name="com.example.backgroundScanning.ScaningService$Reciever" android:enabled="true">
            <intent-filter>
                <action android:name="com.example.action.DEVICE_SCANNED"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>
